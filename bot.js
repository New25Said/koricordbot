import { Client, GatewayIntentBits } from "discord.js"; import http from "http"; import admin from "firebase-admin"; const serviceAccount = JSON.parse( process.env.FIREBASE_SERVICE_ACCOUNT ); admin.initializeApp({ credential: admin.credential.cert(serviceAccount), databaseURL: "https://koricord-a5f4e-default-rtdb.firebaseio.com" }); const db = admin.database(); const PORT = process.env.PORT || 10000; http.createServer((req, res) => { res.writeHead(200); res.end("KoriBot vivo"); }).listen(PORT); const client = new Client({ intents: [ GatewayIntentBits.Guilds, GatewayIntentBits.GuildMessages, GatewayIntentBits.MessageContent ] }); client.once("clientReady", () => { console.log(`Conectado como ${client.user.tag}`); }); client.on("messageCreate", async (message) => { if (message.author.bot) return; console.log( `[${message.guild?.name}] ${message.author.username}: ${message.content}` ); await db.ref("discordMessages").push({ user: message.author.username, text: message.content, server: message.guild?.name || "DM", timestamp: Date.now() }); console.log("Mensaje guardado en Firebase"); }); client.login(process.env.DISCORD_TOKEN);
